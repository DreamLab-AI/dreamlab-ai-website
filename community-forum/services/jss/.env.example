# Base URL for the JSS instance — override at Cloud Run deploy time.
# In production, use the Cloud Run service URL assigned after first deploy.
# Example: https://jss-HASH.us-central1.run.app/
JSS_BASE_URL=https://jss-HASH.us-central1.run.app/

PORT=8080

# Production note: /data/pods is ephemeral on Cloud Run.
# Mount a GCS FUSE volume at /data/pods for persistent pod storage:
#   --volume type=cloud-storage,bucket=YOUR_BUCKET,mount-path=/data/pods
