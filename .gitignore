# Dependencies
node_modules

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.nostr-identities.env

# Build outputs
dist

# Cache and logs
.cache
.npm
npm-debug.log*
logs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.sw?
.DS_Store

# TypeScript
*.tsbuildinfo

# Testing
coverage
test-results/
tests/*.png
tests/*.jpg

# Vite
vite.config.ts.timestamp-*

# Claude Code / Swarm / Agent tooling
.claude-flow/
.swarm/
.claude/
.aqe/
.agentic-qe/
.mcp.json

# Backup directories
.agentdb-global-backup-*/
.agentic-qe-backup-*/

# Wrangler (Cloudflare dev artifacts)
.wrangler/
**/.wrangler/

# Python virtual environments
**/.venv/
**/venv/

# SQLite databases
*.db

# Upstream kit checkout (cloned at KIT_REF for local dev)
kit/

# External reference repos (not part of this project)
external/

# Legacy directories (deleted from repo, guard against re-creation)
community-forum-rs/

# Local experiments (untracked)
wasm-voronoi/
workshop-builder/

# Seed tooling secrets and probes
scripts/seed/.test-keys.json
scripts/seed/probes/

# agentdb RVF init-rot: empty stores touched by agentdb-enabled tooling; ruvector-postgres is the only memory store (agentbox ADR-015)
agentdb.rvf
agentdb.rvf.lock
